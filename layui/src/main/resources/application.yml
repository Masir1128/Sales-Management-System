spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mmall?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: qkmuser
  thymeleaf:
      prefix: classpath:/templates/
      suffix: .html
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
server:
  #端口号
  port: 8081